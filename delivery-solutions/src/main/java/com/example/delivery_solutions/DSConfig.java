package com.example.delivery_solutions;

import android.content.Context;

import com.transistorsoft.locationmanager.adapter.TSConfig;

public class DSConfig extends TSConfig {
    public DSConfig(Context context) {
        super(context);
    }
}
